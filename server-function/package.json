{
  "name": "@binaris/shift-server-function",
  "version": "0.0.0",
  "description": "Serving function of static files",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "build:watch": "tsc -w",
    "lint": "tslint -c ../common/tslint.yml -p .",
    "test": "ava -v dist/test/*.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mime-types": "^2.1.24",
    "mz": "^2.7.0"
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.0",
    "@types/mz": "^0.0.32",
    "@types/node": "^10.14.13",
    "ava": "^2.2.0"
  }
}
