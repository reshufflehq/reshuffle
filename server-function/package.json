{
  "name": "@reshuffle/server-function",
  "version": "0.0.6",
  "description": "Serving function of static files",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && cp -a src/test/examples dist/test",
    "build:watch": "tsc -w",
    "lint": "tslint -c ../common/tslint.yml -p .",
    "test": "ava -v dist/test/*.js",
    "test:watch": "ava --watch -v dist/test/*.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fresh": "^0.5.2",
    "milliseconds": "^1.0.3",
    "mime-types": "^2.1.24",
    "mz": "^2.7.0"
  },
  "devDependencies": {
    "@types/fresh": "^0.5.0",
    "@types/milliseconds": "^0.0.29",
    "@types/mime-types": "^2.1.0",
    "@types/mz": "^0.0.32",
    "@types/node": "^10.14.13",
    "ava": "^2.4.0"
  }
}
