version: 2.1
jobs:
  build_and_test:
    docker:
      - image: circleci/node:10-browsers
    steps:
      - checkout
      - run:
          name: Check for missing change logs
          command: |
            git fetch origin master:refs/remotes/origin/master -a
            node common/scripts/install-run-rush.js change --verify
      - restore_cache:
          key: dependency-cache-{{ checksum "common/config/rush/shrinkwrap.yaml" }}
      - run:
          name: Install dependencies
          command: node common/scripts/install-run-rush.js install
      - save_cache:
          key: dependency-cache-{{ checksum "common/config/rush/shrinkwrap.yaml" }}
          paths:
            - common/temp
      - run:
          name: Build source
          command: node common/scripts/install-run-rush.js rebuild --verbose
      - run:
          name: Run lint
          command: node common/scripts/install-run-rush.js lint
      - run:
          name: Run tests
          command: node common/scripts/install-run-rush.js test --verbose
  update_npm_dependencies:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-tmp-1
      - run:
          name: Install needed npm packages
          command: npm install npm-check-updates @octokit/rest --no-save
      - save_cache:
          key: dependency-cache-tmp-1
          paths:
            - node_modules
      - run:
          name: Configure git
          command: |
            git config user.email "auto-npm-dep-update@shiftjs.com"
            git config user.name "autoupdate"
      - run:
          name: Update npm dependencies
          command: node common/scripts/update-npm-dependencies.js
workflows:
  version: 2
  build_and_test:
    jobs:
      - build_and_test
  nightly:
    jobs:
      - update_npm_dependencies
