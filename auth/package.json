{
  "name": "@reshuffle/auth",
  "version": "0.0.1",
  "description": "Auth related shared helpers",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "lint": "tslint -c ../common/tslint.yml -p . && eslint -c ../common/.eslintrc.json --ext .ts src",
    "test": "echo no tests"
  },
  "keywords": [
    "binaris",
    "reshuffle",
    "auth",
    "identity"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^6.7.2",
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  }
}
