{
  "name": "@reshuffle/utils-subprocess",
  "version": "0.0.2",
  "files": [
    "dist"
  ],
  "description": "Subprocess wrapper",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "lint": "tslint -c ../common/tslint.yml -p . && eslint -c ../common/.eslintrc.json --ext .ts src",
    "test": "echo No tests"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.12.14",
    "eslint": "^6.7.2",
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  }
}
